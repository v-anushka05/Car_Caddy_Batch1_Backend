# Application name
spring.application.name=CarManagement

# Server configuration
server.port=${PORT:8080}              
server.address=0.0.0.0                

# Database configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/carcaddy?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Dileep@9123}
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true             

# Email configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:springboardmentor430@gmail.com}
spring.mail.password=${MAIL_PASSWORD:kkevxzvvrflkrxcg}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.from=${MAIL_USERNAME:springboardmentor430@gmail.com}  # Match authenticated user

# Task scheduling timezone
spring.task.scheduling.time-zone=Asia/Kolkata